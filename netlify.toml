[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# Handle client-side routing - all routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200